export interface techType {
    name: string;
    yrs: number;
}

export interface repoArrayData {
    name: string,
    html_url: string,
    description: string,
    language: string,
    created_at: string,
    updated_at: string,
}